spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
        highlight_sql: true
        default_batch_fetch_size: 100
    show-sql: true

server:
  port: 8080