spring:
  application:
    name: monito

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

logging:
  level:
    com:
      monito: debug

server:
  port: 8080